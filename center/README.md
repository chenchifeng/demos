一些常用的居中方法：
	块状元素，文本，图片的垂直居中和水平居中。

居中的方法还有很多，例如：
     1. 元素容器display：table;
	元素display：table-cell;
	这样就可以用text-align属性去居中了.

     2. 使用transform: translate(-50%,-50%);

     3. 使用vertical-align属性

     4. 使用:before伪元素

但是上面的这些方法比较难理解,容易出错。

