// 把 127.0.0.1 变成 127-0-0-1 这样的格式
var a = '127.0.0.1';

// 先把字符串转换为数组,用 . 作为分隔标志
// 这时候 b = ['127','0','0','1']
var b = a.split('.');

// 再把数组转换为字符串,用 - 作为连接符
var c = b.join('-');

// 输出结果为 127-0-0-1
console.log(c);
